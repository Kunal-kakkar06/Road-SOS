# RoadSOS Infrastructure & Deployment Configs

This directory contains infrastructure setup and deployment configurations for RoadSOS.

## Layout
- `docker/`: Docker Compose and container definitions for production & dev.
- `deployment/`: Vercel configuration (`vercel.json`), Railway configurations (`railway.toml`).
- `monitoring/`: Prometheus and Grafana alerting & metrics collection manifests.
